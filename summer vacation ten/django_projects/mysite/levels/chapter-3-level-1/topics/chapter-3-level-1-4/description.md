請使用 Matplotlib 畫出水果價格長條圖。

資料如下：

fruit = ["Apple", "Banana", "Orange", "Grape"]
price = [30, 45, 25, 40]

請完成 create_bar_chart() 函式，並在函式裡：

1. 使用 plt.bar(fruit, price)
2. 設定標題為 "水果價格"
3. 設定 x 軸名稱為 "水果"
4. 設定 y 軸名稱為 "價錢"
5. 回傳 plt 物件