import threading

# 在這裡寫程式
def play_multimedia(audio_path, video_path, audio_player, video_player):
    # 1. 建立 AudioThread
    audio_thread = None

    # 2. 建立 VideoThread
    video_thread = None

    # 3. 啟動兩個 Thread

    # 4. 等待兩個 Thread 完成

    return "多媒體播放完成"
