## 進階挑戰（選做，額外徽章 +5 點）

請完成 `shortest_path(graph, start, end)` 函式，利用 BFS 找出從 `start` 到 `end` 的其中一條最短路徑（以節點 list 表示，例如 `["A", "B", "D", "E"]`）。若兩節點不連通，回傳 `None`。

**提示**：在 BFS 過程中，除了記錄 `visited`，也用一個 dict 記錄每個節點是「從哪個節點走過來的」（前驅節點），拜訪到終點後再依前驅節點反推回起點，最後把路徑反轉。
